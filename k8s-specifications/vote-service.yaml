apiVersion: v1
kind: Service
metadata:
  labels:
    app: myvote
  name: myvote
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 8080
    targetPort: 80
    nodePort: 31000
  selector:
    app: myvote
  
